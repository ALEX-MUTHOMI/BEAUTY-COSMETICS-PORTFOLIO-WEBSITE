# Local-only security scan profile.
# Placeholder/safe values only. Do not add real provider credentials,
# production secrets, customer data, or real phone numbers.

DEBUG=False
SECURITY_SCAN_MODE=true
SECURE_SSL_REDIRECT=False

ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0,web,testserver,host.docker.internal
CSRF_TRUSTED_ORIGINS=http://localhost:8000,http://127.0.0.1:8000,http://host.docker.internal:8000
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

PAYMENT_PROVIDER_MODE=fake
CHECKOUT_MPESA_PROVIDER=fake
EMAIL_PROVIDER=fake

DARAJA_ENV=sandbox
DARAJA_CONSUMER_KEY=
DARAJA_CONSUMER_SECRET=
DARAJA_SHORTCODE=174379
DARAJA_PASSKEY=
DARAJA_CALLBACK_URL=
DARAJA_TEST_MSISDN=
DARAJA_TEST_AMOUNT=1

LOG_LEVEL=INFO
